<div class="popup_title">	
	<h3 class="left"> View Location</h3>
	<% if can? :edit, :locations %>
  		<button id="edit" class="right jqxButton" data-role="none"> Edit </button>
	<% end %>
	<div style="clear:both">	</div>
</div>
<%= render 'form' %>
