<div class="popup_title">	
	<h3 class="left"> View Distribution Partnership</h3>
	<% if can? :edit, :entity_partnership %>
  		<button id="edit" class="right jqxButton" data-role="none"> Edit </button>
	<% end %>
	<div style="clear:both">	</div>
</div>
<%= render 'form' %>
