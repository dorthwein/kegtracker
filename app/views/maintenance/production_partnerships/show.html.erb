<div class="popup_title">	
	<h3 class="left"> View Production Partnership</h3>
	<% if can? :edit, :entity_partnerships %>
  		<button id="edit" class="right jqxButton" data-role="none"> Edit </button>
	<% end %>
	<div style="clear:both">	</div>
</div>
<%= render 'form' %>
